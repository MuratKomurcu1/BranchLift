services:
  database:
    image: postgres:16
    container_name: shared-db
    network_mode: host
    volumes:
      - /tmp/shared-postgres:/var/lib/postgresql/data
  cache:
    image: redis:7
    volumes:
      - shared-cache:/data

volumes:
  shared-cache:
    external: true
